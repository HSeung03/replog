<?php

return [

    /*
     * 운동 종목 카테고리.
     *
     * 검증 규칙(in:...)에 문자열을 직접 박아두면 카테고리를 하나 늘릴 때
     * 고쳐야 할 곳을 찾아다녀야 한다. 백엔드에서는 여기 한 곳만 본다.
     * 모바일 쪽 대응 목록은 mobile/src/constants/categories.js에 있다.
     *
     * 저장되는 값이 한국어인 것은 초기 설계가 그래서다. 키(chest 등)로
     * 옮기려면 exercises 테이블의 기존 데이터를 함께 옮겨야 하므로
     * 별도 마이그레이션 작업으로 남겨둔다.
     */
    'categories' => ['가슴', '등', '하체', '어깨', '팔', '유산소'],

];
